# This CMakeList is not mandatory, but due to modularity it is good practise to have it in each folder
# Uncomment it if this component is used directly as a project

#idf_component_register(SRCS http_server.c
#					   INCLUDE_DIRS ".")